
<table class="table table-borderd">
<tr>
<th>ClassName</th>
<th>SubjectName</th>
<th>Status</th>
<th>TeacherName</th>
</tr>
<% @subjects.each do |sub| %>
<tr>
<td><%= @batch.batch_name %></td>

<td><%= sub.subject_name %></td>
<td> <%= sub.assign ? sub.assign : "Not assigned" %></td>
<td>

    <%=  sub.teacherclasses.first.teacher.first_name if sub.teacherclasses.present? %>
      </td>
   

 </td>

</tr>
<% end%>
</table>