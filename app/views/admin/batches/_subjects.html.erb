
<table class="table table-borderd">
<tr>
<th>ClassName</th>
<th>SubjectName</th>
<th>Status</th>
<th>TeacherName</th>
</tr>
<% @subjects.each do |sub| %>
<tr>
<td><%= @batch.batch_name %></td>

<td><%= sub.subject_name %></td>
<td> <%= sub.assign ? sub.assign : "Not assigned" %></td>
<td>
 <%#= sub.subject_name.teacherclasses.teacher.first.first_name   %>
<%= sub.teacherclasses.first ? sub.teacherclasses.first.teacher.first_name : "nil" %>

 </td>

</tr>
<% end%>
</table>